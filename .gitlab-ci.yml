image: alpine:latest
before_script:
  - apk update --yes
  - apk add build-essential cmake
  - apk add libmodbus-dev eigen
variables:
  GIT_SUBMODULE_STRATEGY: recursive
build:
  script:
    - cmake .
    - make