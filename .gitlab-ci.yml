image: ubuntu:bionic
before_script:
  - apt-get update --yes
  - apt-get install --yes build-essential cmake
  - apt-get install --yes libmodbus-dev eigen
variables:
  GIT_SUBMODULE_STRATEGY: recursive
build:
  script:
    - cmake .
    - make